@model List<City>

<h1 class="display-5 text-dark">City List</h1>
<hr />
<div>
    <a class="btn btn-sm btn-create" asp-controller="Admin" asp-action="CityCreate">Create City</a>
    @* @if (Model.Count > 0)
    {
    @if (ViewBag.IsDeleted)
    {
    <a class="btn btn-sm btn-danger" asp-controller="Admin" asp-action="AllTripList" asp-route-isDeleted="false">Show Undeleted Products</a>
    }
    else
    {
    <a class="btn btn-sm btn-danger" asp-controller="Admin" asp-action="AllTripList" asp-route-isDeleted="true">Show Deleted Products</a>
    }
    }
    else{
    <a class="btn btn-sm btn-danger" asp-controller="Admin" asp-action="AllTripList" asp-route-isDeleted="false">Show Undeleted Products</a>
    <a class="btn btn-sm btn-danger" asp-controller="Admin" asp-action="AllTripList" asp-route-isDeleted="true">Show Deleted Products</a>
    }

    *@

</div>
<hr />
@if (Model.Count > 0)
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">City Name</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var city in Model)
            {
                <tr>

                    <td>@city.CityId</td>
                    <td>@city.CityName</td>
                    <td>
                        <a asp-controller="Admin"
                   asp-action="CityEdit"
                   asp-route-id=@city.CityId
                   class="btn btn-sm btn-edit">Edit</a>
                        <button type="button" class="btn btn-sm btn-continue fw-bold btn-delete" data-bs-toggle="modal" data-bs-target="#DeleteModal_@city.CityId">
                            Delete
                        </button>
                        <div class="modal fade" id="DeleteModal_@city.CityId" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLabel">Warning!</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        <p class="text-danger">This city will be deleted!</p>
                                        <p>Are you sure?</p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No</button>
                                        <a asp-controller="Admin"
                                   asp-action="CityDelete"
                                   asp-route-id=@city.CityId
                                   class="btn btn-danger">Yes</a>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </td>


                </tr>
            }
        </tbody>
    </table>
}
else
{

    ;
}
